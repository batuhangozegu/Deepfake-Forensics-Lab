package com.deepfake.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // RestTemplate is created directly in AnalysisService — no bean needed.
}
